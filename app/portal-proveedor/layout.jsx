export default function PortalProveedorLayout({ children }) {
  return (
    <div data-app="portal-proveedor" className="bg-[var(--background)] text-[var(--foreground)] min-h-full">
      {children}
    </div>
  );
}
